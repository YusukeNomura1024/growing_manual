<% if admin_user_signed_in? %>
  ブックマーク数<%= manual.bookmarked_users.count %>件
<% else %>
  <% if manual.bookmarked_by?(current_user.id) %>
    <%= link_to '♥ブックマーク解除', manual_bookmark_path(manual), method: :delete, remote: true, class: "btn btn-outline-bookmark btn--small" %>
  <% else %>
    <%= link_to '♡ブックマークする', manual_bookmark_path(manual), method: :post, remote: true, class: "btn btn-outline-bookmark btn--small" %>
  <% end %>
<% end %>